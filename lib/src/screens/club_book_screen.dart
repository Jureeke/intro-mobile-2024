import 'package:flutter/material.dart';

class ClubBookScreen extends StatelessWidget {
  static const routeName = '/home/profile/posts';
  const ClubBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: const Center(
        child: Text('ClubBookScreen'),
      ),
    );
  }
}
