import 'package:flutter/widgets.dart';

class PlayOpenMatchScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Play an open match Screen',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
